import styles from "./AppShell.module.css";

export default function Footer() {
  return (
    <footer className={styles.siteFooter}>
      <div className="container">
        <p>Culture Finder — find local cultural events</p>
      </div>
    </footer>
  );
}